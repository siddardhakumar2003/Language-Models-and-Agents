{
  "language": "telugu",
  "training_phase": "pretraining",
  "batch_size": 32,
  "learning_rate": 1e-4,
  "weight_decay": 0.01,
  "warmup_steps": 5000,
  "total_steps": 500000,
  "eval_steps": 5000,
  "checkpoint_steps": 10000,
  "optimizer": "adam",
  "scheduler": "cosine_with_warmup",
  "loss_function": "cross_entropy",
  "gradient_accumulation_steps": 1,
  "max_grad_norm": 1.0,
  "seed": 42,
  "device": "cuda",
  "num_workers": 4,
  "mixed_precision": "fp16",
  "description": "Training configuration for Telugu pretraining"
}
